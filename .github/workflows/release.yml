# 이 워크플로우는 kbeauty 프로젝트에서는 필요하지 않습니다.
# 원본 Medusa.js 프로젝트의 릴리즈 워크플로우였으나,
# kbeauty 프로젝트에서는 사용하지 않으므로 비활성화됩니다.

name: Release (Disabled for kbeauty)

# 절대 실행되지 않도록 조건 설정
on:
  workflow_dispatch:  # 수동 실행만 가능

jobs:
  disabled:
    runs-on: ubuntu-latest
    # 항상 건너뛰기
    if: false
    steps:
      - name: Disabled
        run: echo "This workflow is disabled for kbeauty project"
